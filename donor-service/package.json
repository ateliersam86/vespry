{
  "name": "vespry-donor-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vespry — mur des soutiens. Cloudflare Worker qui ingère les webhooks Ko-Fi et GitHub Sponsors et expose le flux des donateurs.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.2",
    "wrangler": "^4"
  }
}
